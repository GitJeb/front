
    <div class="container">
        <div class="row">

            {{#each uploads as | upload |}}
                <div class="col-md-4">
                    <div class="thumbnail">
                        <!-- pic display -->
                        <div class="pic-display">
                              {{#fileType upload}} {{/fileType}}
                        </div>
                    </div>
                </div>
            {{/each}}

          </div>


        </div>
      </div>
